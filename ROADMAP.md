# Game Roadmap

This document tracks the concrete, sequenced engineering work — what's done, what's next, and why each step is ordered the way it is. The project's goals and scope discipline, performance methodology, and long-lived technical decisions now live in their own documents, so this one stays a working task list:

- **[VISION.md](VISION.md)** — project goals, scope discipline (why the plan is sized the way it is), and the Long Term wish list.
- **[PERFORMANCE.md](PERFORMANCE.md)** — `grid_bench` numbers, measurement methodology, and the mistakes that methodology exists to prevent.
- **[ENGINEERING_NOTES.md](ENGINEERING_NOTES.md)** — deferred technical decisions and the reasoning behind them.
- **[README.md](README.md)** — how to build, run, and test the game, including the Manual Tester Checklist.
- **`notes/`** — informal lore and feature brainstorming, upstream of anything here.

### 🚩 Definition of Done — v0.1 Vertical Slice
The single milestone that matters. Everything before Long Term (VISION.md) serves this:

> The player enters **one** quantum world, uses physics-based movement and destruction to complete **one** objective type, extracts successfully or dies losing the run, and their pet ML agent visibly gains from the run and earns coins idly.

If that loop is not fun, no amount of factories or stock markets will save it. If it *is* fun, it is a demo worth showing and a foundation worth expanding.

## 🟢 Current Status
- [x] Initial GitHub Repo and CMake build system setup.
- [x] Barebones C++/SDL2 pixel physics engine prototype built (Sand, Water, Wall interactions).
- [x] **Data-driven material system.** Materials are rows in a table (`src/physics/material.h`), not branches in the update loop. Four generic behaviours — Static / Powder / Liquid / Gas — drive all movement, with density deciding what sinks through what.
- [x] **Seven materials** proving the above: Sand, Water, Wall, Wood, Oil (floats on water), Steam (rises), Fire. Each cost one table row, no engine changes.
- [x] **Fixed timestep.** Simulation advances at a constant 60 Hz independent of render framerate, so the game behaves identically on a 60 Hz and a 144 Hz display.
- [x] **Headless test harness.** The simulation has no SDL dependency and is covered by tests wired into CTest — conservation of matter, density stratification, static materials, border sealing. Now six suites, one per concern: `tests/test_grid.cpp`, `tests/test_player.cpp`, `tests/test_tool.cpp`, `tests/test_collapse.cpp`, `tests/test_run.cpp`, `tests/test_scene.cpp`, sharing one harness in `tests/test_util.h`. 179 checks in total.
- [x] **Chunked dirty-rect updates.** The world is split into 64x64 chunks, each tracking the bounds of the cells in it that can still move. Settled chunks are skipped entirely, so cost now scales with how much is *moving* rather than with world size. Every write wakes its 3x3 neighbourhood so nothing is left hanging in mid-air, and the awake-chunk count is on screen so culling bugs are visible rather than silent.
- [x] **Benchmark.** `tests/bench_grid.cpp` measures a 960x540 world across six scenarios against the 60 Hz budget. Deliberately not a CTest test — timings inform, they do not gate. Every scenario has to be built so its subject never falls asleep inside the measurement window, or the number reported is mostly the cost of an idle world: `cascading` scrapes the floor and pours in at the ceiling, `burning` drips fresh Fire, and `collapsing` drains landed slabs away and feeds in replacements.
- [x] **Reactions.** A data-driven `REACTIONS` table (`src/physics/reaction.h`, catalyst + target -> result, rolled per step) drives Fire: it ignites touching Wood and Oil, is doused into Steam by Water, and burns out on its own. Second and last major engine axis — transformation is now as data-driven as movement. The one non-obvious piece: Fire's self-decay is *spontaneous* (no neighbour required), which means it has no movement to piggyback a wake-up on, so a boxed-in Fire cell self-marks its own neighbourhood dirty every step purely to avoid freezing mid-burn. Covered by 5 tests in `tests/test_grid.cpp`, including a dedicated regression test for that freeze case.
- [x] **Player Character + Player Physics.** A 4x8 rigid body (`src/physics/player.h`) that is *not* a cell: it has its own position and velocity and only ever reads the grid to ask whether a cell is solid. Position is an integer cell plus a sub-cell remainder rather than a float, so collision compares whole cells and the float-edge bugs never arise; movement resolves one cell at a time per axis, which makes tunnelling impossible by construction rather than by being fast enough. Solidity is derived from the existing `MoveKind` (Static and Powder are solid, Liquid and Gas are not), so it stays one table, not two. Gravity, jumping, a 2-cell step-up for walking over uneven powder, and an unstuck search for when falling sand buries the player. Covered by 18 tests in a second suite, `tests/test_player.cpp`.
- [x] **Structures fall as rigid bodies.** Wall and Wood no longer hang in mid-air when the ground is dug out from under them. An unsupported piece falls **as one rigid body, keeping its shape the whole way down** — not as loose grains, and that distinction is the whole feature: the first implementation dissolved an unsupported slab into a Rubble powder, which read as a different bug, because masonry that turns to gravel the instant it comes free looks broken rather than physical. The piece stays in the cell grid while it falls, so it is a rigid body only in *how it moves*, not in where it lives — which is what keeps rendering, player collision, digging and fire working on it with no changes at all. Structure is a `structural` flag in the `MATERIALS` table, so it stays one table rather than a second list. Four decisions are load-bearing and each is a trap if reversed: support is checked **on disturbance only**, never as a global truth; pieces over 4,096 cells are **assumed supported** rather than judged, because a missed fall is invisible while a wrong one drops the level; **a fill records what it concluded, not just where it went**, or a wall on solid ground sheds chunks of itself; and a falling piece **accelerates by falling repeatedly rather than further**, which is what makes it structurally unable to step over a floor thinner than its speed. Full reasoning for all four is in [README](README.md#structures-and-falling) and is deliberately not repeated here. Covered by 31 tests in a fourth suite, `tests/test_collapse.cpp` — including a disc whose exact shape is compared before and after landing (a rectangle still looks like a rectangle after a row of it slips, so rectangles alone were not catching this) and a piece at full speed that must land *on* a one-cell shelf rather than through it. Costs 1.67 ms with 3,200 cells of structure in the air at once, and nothing at all when nothing is falling.
- [x] **Basic Interaction — digging.** The player can now change the world, which is the line between a sandbox and a game. A dig is a ray marched one cell at a time from the body's centre to the first solid cell, which is then blown out to a small radius (`src/physics/tool.h`). Deliberately *not* a method on `Player`: keeping the body's grid reference `const` is what makes it structurally incapable of breaking the `set_element` write rule, so the verb lives in its own module that takes a mutable `Grid&`. What blocks a shot is the same `is_solid` the player collides against — one definition used twice, so terrain and powder stop a ray while water and fire do not. Range is real distance rather than a step count, and the cooldown is in fixed steps rather than seconds. Covered by 23 tests in a third suite, `tests/test_tool.cpp` — including the anti-tunnelling case (a thin wall must not be dug *through* to the terrain behind it) and the collapse case (digging out the base of a settled pile makes it fall, which only works because removal goes through `set_element` and wakes the neighbourhood).

Performance numbers, benchmark methodology, and the measurement mistakes that methodology exists to prevent have moved to **[PERFORMANCE.md](PERFORMANCE.md)**. Read it before trusting any timing claim in this document — several already had to be corrected in place after a better measurement contradicted them.

## 🔴 Short Term (one open question, not the next work)
*One engine item left, and it is the one that was always conditional — it should be settled by playing rather than by argument, and it may well be settled as "no". **Foundations below is what comes next**, which is why this section is no longer titled "Next Up". Neither blocks the other: if playing has not answered this question yet, go start F1.*

- [ ] **Player displaces material.** The grid does not know the player exists, so sand falls straight through the body and the unstuck search cleans up afterwards. Fixing it properly means the player pushing material out of the way, which is a real design problem — it has to happen without destroying matter. Now that digging exists, the question is answerable by playing: if the artifact is obvious in practice, do it; if it isn't, leave it. See `ENGINEERING_NOTES.md`.

## 🟠 Foundations (before Medium Term)

*Four items that are not features. Each one is cheap now, gets steadily more expensive as more systems are built against the current shape, and is a prerequisite for **several** Medium Term items rather than one — which is why they are pulled out here instead of left implicit inside the slice.*

*This section is not an invitation to extend the engine, and its length is a budget rather than a starting point. Six pillars are already in place — data-driven materials, data-driven reactions, the fixed timestep, the SDL-free simulation, `set_element` / `paint` / `swap_elements` as the only write path, and chunked updates. Nothing below adds a seventh axis to the simulation. **Read that sentence as a statement about F1–F4, which is what it was written about, and not as a standing prohibition** — the section that follows Foundations spends a seventh axis on purpose (E2, heat), bought by an observation from play rather than by an itch, and says so in full where it does it. Two of the four are groundwork the slice needs no matter which way the open design questions are answered; the other two were already written down elsewhere in this document and have moved here, not grown — F3 was the first Medium Term item, and F4 was a prerequisite buried inside a Presentation & Tooling entry, where it was unreachable behind that section's own "do not start this yet" rule.*

*Order: F1 → F2 → F3 → F4. F2 wants F1's seed and F2.3 finishes what F1 starts, so those two are really one piece of work, roughly a session's worth. **F3 comes after F2 because both of them rewrite `main.cpp`** — F2.1 moves the run state out and F2.3 moves input handling out, so doing F2 first leaves F3.2 with strictly fewer coordinate sites to convert, and doing it the other way means converting sites that are about to move. F4 is genuinely independent and is last because it is the only one of the four that is not pure groundwork: it produces something visible, which makes it the easiest to keep polishing past the point of being done.*

*One convention for the steps below: **they point at code by name — a function, or a line quoted out of it — never by line number.** F1.1 on its own shifted `grid.cpp` by five lines, `grid.h` by seventeen and `main.cpp` by fourteen, which silently falsified every numeric reference in this section including the one describing the bug F1.1 had just fixed. A plan read across many sessions cannot use an anchor that moves every time the plan is executed.*

### F1 — Determinism, first half: the simulation
*The one gap that was not written down anywhere in this document.*

`Grid` used to seed `std::mt19937` from `std::random_device` inside its own constructor, with no way to set the seed and no way to read it back, so **no run was reproducible** — not by a player, not by a bug report, and not by the test suite. F1.1 settled the ownership half of that; the generator itself is what remains. The test suite is still paying for it: `test_grid.cpp` runs 150-200 steps in places and asserts that failure is *astronomically unlikely* rather than asserting an outcome, which is a workaround for nondeterminism that F1 exists to retire.

Four Medium Term items depend on this and none of them says so. Quantum Worlds cannot regenerate or share a trial. Save and persistence has to either serialise 2.5 KB of generator state or accept that loading diverges from the run that was saved. The playtest gate collects bug reports that cannot be reproduced, which is the worst kind to receive from someone who is not you. And health/death cannot be balanced against a fixed scenario if the scenario is never the same twice.

**F1 alone does not deliver any of those four, and it is important not to tick this section off believing it did.** F1 makes `Grid` a pure function of its seed. The *game* is still not reproducible after F1.7, because input does not enter the simulation cleanly: the brush is stamped once per **rendered** frame, outside the fixed-step loop entirely, and the keyboard is sampled once per rendered frame and then applied to *every* fixed step in that frame — so a held dig issues two commands at 30 fps and one at 144 fps. Same seed, same physical keypresses, different world. **F2.3 is the second half**, and only the two together produce the thing the four items actually need, which is not a seed but a seed plus a replayable input log.

**The decision: a stateless hash, not a seed argument.** The smaller change is to pass a seed into the existing generator. The better one is to delete the generator and derive randomness from a hash of `(cell position, step, seed, salt)`. It settles three things at once — determinism, a world with no generator state to serialise, and the **RNG cost** entry in `ENGINEERING_NOTES.md`, which is a performance note about exactly this code. Doing it as a seed first and a hash later means touching every call site twice.

There are five call sites, all in `grid.cpp` and all of the form `rng()`: colour jitter in `jittered_color`, one direction pick each in `step_powder` and `step_fluid`, the chance roll in `try_react`, and the per-row sweep direction in `update`. Take them one at a time, running the suite after each. **Do not carry the number five forward as a fact** — the durable version of it is the invariant, that no call to the generator or the hash lives outside the one helper that owns it, which is what F1.6 is able to check mechanically once `<random>` is gone from the header.

- [x] **F1.1 — Take a seed, keep the generator.** `Grid` now takes a `uint64_t seed` (defaulted to `Grid::DEFAULT_SEED`) and exposes `seed()`. `std::random_device` is gone from `grid.cpp` entirely and lives in `main.cpp`, which is now the only nondeterministic line in the project and prints the seed it chose — a seed that cannot be read back is only half of determinism. Three checks rather than the two planned, in `test_grid.cpp` (100 total): same seed matches, **different seed diverges**, and the whole 64-bit seed is used. The second is what makes the first mean anything, since two worlds also match when nothing random ever happened. The third came out of the implementation: `std::mt19937` seeds from 32 bits, so handing it the seed directly would silently discard everything above bit 31 — it goes through a `std::seed_seq` of both halves instead. **Side benefit worth knowing:** `grid_bench` used to draw a fresh random seed per run, so consecutive runs simulated slightly different worlds. They are now identical, which removes one small source of the run-to-run spread the measurement rules above exist to work around.
- [x] **F1.2 — Add a wide step counter.** `Grid` now carries a `uint64_t step_count`, read back through `steps()` — the same split as `world_seed` / `seed()`. `frame_tag` is one byte and cannot be the time input to a hash: randomness would repeat for a given cell every 256 steps, which is visible periodicity, not noise. **This step's own instructions were wrong about where to put the increment and are corrected here rather than quietly.** They said to increment beside `++frame_tag`, for the stated reason that everything in a step should agree on which step it is — but `++frame_tag` sits *below* `resolve_support()`, so doing that would have given the support resolve the previous step's number and the sweep the current one, which is precisely the inconsistency the instruction was trying to avoid. The increment therefore goes at the very top of `update()`. `frame_tag` cannot follow it up there: it is stamped after support resolves on purpose, so that cells a falling structure just moved are not marked as already updated and still get their turn in the sweep. The two are different questions wearing the same shape, which is the whole reason this step exists — and the frame-tag wraparound note is now strictly about the skip flag it was always about. Three checks (103 total), because a counter nothing observes can be quietly wrong until the thing depending on it is built, at which point the bug looks like it is in the new code: the clock starts at zero, counts every update, and **keeps counting through steps where the world is empty and no chunk wakes** — it has to measure time rather than activity, or two runs paused for different lengths would disagree about when it is.
- [x] **F1.3 — Introduce the hash, move one call site.** `src/physics/random.h` holds a splitmix64 finalizer over `(seed, step, index, stream)` with `coin`, `chance(pct)` and `spread(range)` on top; `Grid` gets thin inline wrappers that fold in the seed and step so a call site names only where it is asking from and which decision it is making. The **sweep direction** in `Grid::update` is moved; the other four follow in F1.4. **One correction to this step as written: it claimed the sweep "needs no salt" because its input is already a row and a step.** That is wrong — row `y` and cell index `y` are the same number, and cell index `y` is a real cell, so with no stream tag a row's direction would be drawn from the same value as that cell's own decisions. It has a tag. The tag was free and the collision would not have been, which is the argument for stream separation in miniature. Seven checks (110 total), and they test the mixer **directly rather than through the world**, which is the part worth keeping: the failure mode that matters is a mixer that is merely *poor* — biased, correlated between streams, or repeating for a cell across steps — and such a mixer still produces a world that settles, stratifies and burns exactly as every other test expects. It only looks wrong in motion, which no assertion about the world can notice.
- [x] **F1.4 — Move the remaining four.** The direction picks in `step_powder` and `step_fluid` and the chance roll in `try_react` are on distinct streams, each keyed on the cell's own index. **Colour jitter in `jittered_color` is the one that is different**: it is a one-time authored value, not a per-step decision, so it hashes on `(seed, index)` with the step pinned at 0 — through its own `authored_spread` wrapper, so the exception is visible in the header rather than hidden at the call site. The deliberate visible change: repainting a cell in the same spot now produces the same shade instead of a new one. Cells still carry their colour when they move, because `swap_elements` moves the whole `Element`, so a falling grain keeps the shade it was painted with rather than flickering to whatever its new index would hash to. Two new checks as planned (112 total): jitter is still live (20 distinct shades across 32 neighbouring cells, not flattened to the table colour), and a cell erased, stepped past and repainted comes back identical. `rng()` now appears nowhere in `src/`; the member itself goes in F1.6.
    - **This step turned up a real test bug that had nothing to do with it, and the sequence is worth keeping.** `fire ignites adjacent wood` went to 0/30. The reaction odds were fine — the trials were not. `count_ignitions` builds thirty sealed pairs that are identical down to the cell, and since F1.1 made the default seed fixed they had all been the *same world*, so "thirty independent trials of a 2:1 race" could only ever return 0/30 or 30/30. It happened to return 30/30, so it passed, so nobody looked. F1.4 changed the numbers, that one world lost its race, and the same broken test failed loudly. **The test was equally wrong before and after; only its answer changed.** Each trial now takes its own seed and the results read like samples again — wood 16/30, oil 28/30, against bars of 12 and 18. Two things follow: a determinism change can expose an unrelated latent defect, so a failure straight after one is not automatically caused by it; and a fixed seed makes a suite quieter without making it stronger, so anywhere a test repeats a trial for statistical weight, **the repetition has to vary something** or the seed has silently collapsed it to one sample.
- [x] **F1.5 — Reserve the stream separation, do not build it yet.** The rule is written down in `random.h` next to the thing it governs, before there is a generator to break it: world generation draws only from streams minted by `worldgen(n)`, the simulation only from the declared tags, neither borrows the other's. Without it, generating one extra cave shifts the values the simulation reads for those same cells, so a terrain tweak silently alters how sand falls somewhere it never touched — and that is indistinguishable from a bug in the sand. **Streams are minted from one base constant rather than hand-written**, so the generator can take as many as it needs without anyone picking further magic numbers by eye. Two notes left for whoever writes it: generation is authored, so it passes **step 0** like colour jitter does, and `index` means "which thing am I asking about", not necessarily a cell. Still one function and one constant in total — with a stateful generator this would have been a second generator to own, seed, save and keep in lockstep.
    - **The check is a `static_assert`, and the reason it is not a test is the point of the step.** Two streams sharing a value are one stream, silently: the world still runs, it just carries a correlation no assertion is placed to catch. So `random.h` lists every declared stream in `SIM_STREAMS` — the duty `MATERIALS` already has towards `ElementType`, enforced the same way — and proves at compile time that they are pairwise distinct, along with the first sixteen minted generation streams. **Verified by temporarily duplicating a value and confirming the build fails**, because an assertion nobody has seen fire is an assertion that might be vacuously true. Two runtime checks alongside it (114 total), covering what the assert cannot: distinctness is the weak half, since two tags one bit apart are distinct and still correlated, so the minted streams are also held to the same ~50% disagreement bar as the declared ones — against the simulation and against each other.
- [x] **F1.6 — Delete `std::mt19937` and `<random>` from `grid.h`, then measure.** Both are gone; `Grid` now stores nothing but the two numbers that are its whole state, `world_seed` and `step_count`, and the constructor has nothing left to seed. Bracketed A/B, five runs each rather than three, alternating hash/generator throughout: `churning`'s ranges overlap (no effect — this is what "below the noise floor" looks like when it is actually true), but `cascading`'s do not, and the hash is the slower side of that gap by about 1.7–1.9%. **Recorded honestly rather than as the win it was filed as**, in `PERFORMANCE.md` and the RNG entry in `ENGINEERING_NOTES.md`, both corrected in place. F1 was never conditional on this paying for itself — determinism is the reason F1 exists, and it does not stop being the reason because the side benefit didn't land.
- [x] **F1.7 — Docs.** README's Engine Architecture gets a new `### Determinism` section, placed after Reactions and before The player, alongside the write rule and the wake rule it already documents. States what is actually true — `Grid` is a pure function of its seed, every draw goes through `Grid::coin`/`Grid::chance` the same way every write goes through `set_element`/`swap_elements`, streams keep same-step decisions from correlating, colour jitter is the one exception and why — and is explicit about what is *not* yet true: the game as a whole, because input still enters through the render loop rather than a per-step log, which is F2.3. Points at the RNG entry in `ENGINEERING_NOTES.md` for the measured cost rather than repeating it.

**F1 is complete.** `Grid` is a pure function of its seed; no generator, no state, no `<random>` in the header. That is the first half of determinism, not the whole of it — the four Medium Term items that motivated this section still cannot be started, because they need the *game* reproducible and F2.3 has not landed. What F1 leaves behind: a random helper anyone can extend without relearning its rules (add a `Stream`, key on the right index), a step counter something can finally depend on, and a section of this document that had never once been fully consistent with the source until the anchoring convention above forced it to be.

### F2 — Something owns the run

`Grid`, `Player` and `DigTool` are three unrelated locals in `main()`, declared next to `accumulator`, `current_brush` and `brush_size` with nothing distinguishing run state from shell state. Four separate items across this document are blocked on the same missing type: the world reset hotkey resets *a run*, player death restarts *a run*, save and persistence serialises *a run*, and Quantum Worlds swaps *a run*. Right now each of those is independently "remember to also reset that other variable", which is a bug class that grows with every system added.

It is three members today. The point is to fix this while it is still bookkeeping and not yet a refactor.

- [x] **F2.1 — Create `src/game/run.h` / `run.cpp` and move the three locals into it.** `main.cpp` now holds one `Run` — `grid`, `player` and `dig_tool` are its public members, constructed together from `(width, height, seed)` the same way `main.cpp` built them before, so every call site became `run.grid`, `run.player`, `run.dig_tool` and nothing about the update order or the values passed around changed. SDL-free, added to `ENGINE_SOURCES` in `CMakeLists.txt` next to the other three, for the same reason `src/physics/` is: a run that needs a window cannot be driven by a test. *Verify:* clean build with no new warnings, all four suites still pass at 114 checks — unchanged, because nothing about what runs changed, only where it lives — and the exe launches and stays up rather than crashing on construction order.
- [x] **F2.2 — `Grid::reset(seed)` and `Run::reset(seed)`.** **One correction to this step as written: the signature is `Grid::reset(seed)`, not `Grid::reset()`.** The hint list below never mentioned `world_seed`, which was an oversight rather than a decision — leaving it out of the wipe would mean a "reset" grid kept simulating its *old* seed's randomness, and "a reset run and a fresh run with the same seed are byte-identical" would not even be a meaningful sentence to test, since the two could disagree about which seed they were on. `world_seed` is cleared alongside everything else, and taking it as a parameter is what makes `Run::reset(seed)` able to hand the player an actual new world rather than a re-cleared old one. The wipe clears **every mutable member**, directly and by hand — deliberately not `*this = Grid(width, height, seed)`, because that shortcut is compiler-generated and cannot forget a member, which would make the whole point of the test below impossible to demonstrate. Alongside `cells`, `pixels` and the two dirty-rect vectors there are `pending_support`, `support_stack`, `support_component`, `support_visit`, `support_state`, `support_epoch`, `resolving_support`, `frame_tag`, `step_count` and now `world_seed` — nine members past the two everyone would think of first. **Treat that list as a hint and the test as the authority**, because the next member added to `Grid` will not appear in it. `Run::reset(seed)` calls `grid.reset(seed)` and replaces `player`/`dig_tool` outright, since neither owns anything a wipe needs to preserve.
    - *Verify, three parts.* A scene is built without ever calling `update()` — a floating Wood block with one cell knocked out of it, which queues a support check immediately on removal — so the check "reset clears the queued support check" is proven against a queue known to be non-empty going in, using a new accessor, `has_pending_support_checks()`, added for the same reason `active_chunk_count()` was: some internal state only a test can see. After `reset`, every cell is confirmed `Empty` and every chunk asleep. Then the assertion that actually earns its keep: a grid is dirtied, reset, and driven through the same scripted scene (`build_mixed` + 100 steps) as a completely fresh grid on the same seed, and the two are compared. Comparing immediately after `reset()`, while both are still `Empty`, would not have caught a stale `step_count` — two empty worlds look the same regardless of what their clocks say. Stepping both afterward is what turns "looks the same" into "computes the same," because a wrong `step_count` only shows up once something reads it. **Confirmed this actually catches something**, not just asserts it would: commented out the `step_count = 0` line, watched `a reset run matches a fresh run built with the same seed` fail, put the line back. Six checks (120 total).
- [x] **F2.3 — A POD `Input` struct and `Run::step(const Input&)`. This is the second half of determinism.** `Input` lives in `src/game/run.h` next to `Run`: `left`/`right`/`jump`/`dig`, a shared `cursor_x`/`cursor_y` (the dig aim and the brush centre were always the same mouse position, so one pair covers both), and the three brush fields (`brush_active`, `brush_type`, `brush_size`). `Run::step()` paints the brush, then steps the grid, then the player, then the dig tool — the same order `main.cpp` ran them in before this step, with the brush moved from *before the whole fixed-step loop* to *the first thing inside one iteration of it*. `main.cpp` still samples SDL exactly once per rendered frame — it has no way to do otherwise, there is no such thing as input for a step that has not happened yet — but now calls `run.step(input)` once per fixed step the frame's elapsed time accumulated, so what used to be "paint once per frame, however many steps that frame turns out to buy" is now "paint once per step, whichever frame it lands in." `Run::FIXED_DT` replaces the copy that used to live in `main.cpp`, since `Run` is what actually advances by it now. Player's own `PlayerInput` is left untouched and built from `Input`'s fields inside `step()` — the brush is a run-level concern the player has no business seeing.
    - *Verify.* A new fifth suite, `tests/test_run.cpp` (10 checks, 130 total) — the first in the project driving player, tool and grid together through one entry point rather than one at a time, because `Run::step()` itself is what's under test, not any one subsystem's physics. A scripted sequence lands the player, walks it, jumps it, and digs a hole, all through `run.step(input)`; a second block confirms the brush actually reaches the grid through the same call, painting a circle rather than a single cell. **Then the assertion that actually closes F1:** the same seed plus the same recorded `Input` sequence, replayed through two separate `Run`s — one driven straight through, one consumed in batches of 7 with a render-style read (`get_pixels()`, `active_chunk_count()`) between batches, mimicking exactly what `main.cpp` does between groups of fixed steps every frame — produces a byte-identical grid and an identical player position. That is the property save files, replays and reproducible bug reports all rest on, and until this check existed the project did not have it.
    - **One bug in the test itself, worth recording because of what it looked like.** The scripted sequence originally walked the player toward the world's edge and placed the dig target relative to wherever it ended up, in an 80-cell-wide grid too narrow for that math — the target landed a few cells past the border. It still "passed" its own sanity check ("the dig target is actually solid"), because `get_element()` reads *anything* out of bounds as `Wall` by design, border included — so the check was quietly reading the world's edge, not the block it thought it had placed, and would have read `Wall` after a real dig too, since the border cannot be dug through. Widened the grid so the walk never reaches it. The failure mode worth remembering: an out-of-bounds accident here would not have looked broken, it would have looked like a passing test.
    - **A single-step assertion that turned out not to be true, and correctly wasn't asserted the first time either.** Tried checking `!is_on_ground()` immediately after one jump step; it does not hold; leaving a floor takes a whole cell of upward movement and whether one step's `rem_y` carries that far is `Player`'s own integration detail. `tests/test_player.cpp`'s existing jump test never made this assertion either — it checks the eventual peak height and the eventual landing, which is the more robust claim and the one this suite now makes too. Checked only the velocity flip on the single step, which *is* this step's own responsibility.
    - **Manual verification done against the real window, not just the exe staying up.** Launched `SlopPhysics.exe` and drove it with synthetic input (`SetForegroundWindow` plus `keybd_event`/`mouse_event`, screenshotted between actions) rather than trusting the rewritten input path on the strength of the automated suites alone. Confirmed, visually and via the live title bar: holding right/left actually walks the player through `run.step()`; right-click-dragging paints a real streak of falling Sand that jitters in colour, settles into a slope, and takes the awake-chunk count from 0 to 2 and back to 0; and left-click digging from in range visibly erodes the pile exactly where aimed, cooldown and all. This covers the specific thing F2.3 changed — brush and input now flowing through one `Input`-driven call — which is exactly what the automated suite could not see. It does not replace a full pass of the Manual Tester Checklist (README.md — reactions and structures were not re-exercised here, since neither was touched by this step); that is still owed before the v0.1 slice ships, on its own schedule.
- [x] **F2.4 — Confirm the payoff rather than assuming it.** No code in this step, on purpose — sketching the two dependent items against what F2 actually built, not implementing either early.
    - **World reset hotkey:** genuinely trivial now. `Run::reset(seed)` already does everything but choose the seed and bind the key, so the item is one `SDLK_r` case calling it — a couple of lines, exactly as predicted. Confirmed rather than assumed: nothing about `reset(seed)`'s shape needs a wrapper or a second thought to reach for from `main.cpp`. The seed question that item's own text raises (reuse the current one vs draw a fresh one) is correctly *its* decision to make, not this step's — F2 built the primitive, not the policy for using it.
    - **Pause and single-step:** also small, and for a specific reason worth naming — before F2.3, "the fixed step" was three separate calls inline in `main.cpp`'s loop body, with no single thing to hook a pause around. Now it is one call, `run.step(input)`, so pausing is skipping that call and single-stepping is calling it exactly once outside the loop. The accumulator pitfall this item's own text already warns about (must not keep growing while paused, or unpausing dumps a queued burst) has an equally small fix: don't add `frame_time` to it on a paused frame, same shape as skipping the call itself. **One real design question surfaced that the item's current wording doesn't address, worth having on record before it's implemented rather than discovered mid-step:** should the brush still paint while paused? A pause that also freezes editing forecloses using pause *to* set up a scene precisely, which is a plausible reason someone reaches for `P` in the first place. Left open on purpose — this step confirms shape, not policy, same as the reset hotkey's seed question above.
    - **Conclusion: neither item's shape is wrong.** Both are small changes with a design already dictated by F2's existing pieces, not open architecture. Nothing here needed the "another ten minutes" the step allows for — F2 is done.

The Manual Tester Checklist — run after any change touching `src/physics/`, `src/game/` or `main.cpp` that the automated suites can't fully exercise — has moved to **[README.md](README.md#manual-testing)**.

### F3 — Camera and world-space coordinates
*Moved here from Medium Term, where it was already listed first. Prerequisite for everything in the slice.*

*Written before F3 landed, and kept in the present tense it was written in — the state described below is what F3 was aimed at, not what is true today. F3.1 separated world size from window size and F4.3 moved the world to 640x400 against a 200x150 viewport, so the world is now genuinely larger than one screen and the camera genuinely pans.*

The world was exactly the window: `GRID_WIDTH` was `WINDOW_WIDTH / PIXEL_SCALE`, so a level could not be larger than one screen. A "trial" the size of a 200x150 screen is not a level, it is a room.

**The cost is not the camera math.** It is that the conversion between world and screen is *scattered* — `main.cpp` divides the mouse by `PIXEL_SCALE` to get the brush and aim cell, multiplies by it again to place the player's rect, and again to place the aim mark — and world size and window size are currently the same number wearing two hats. Five sites now, and every future system that reads the mouse or draws a thing adds another. This is the item whose retrofit cost genuinely compounds, which is why it is a foundation and not a feature.

- [x] **F3.1 — Separate world size from window size.** `GRID_WIDTH` / `GRID_HEIGHT` in `main.cpp` are now their own constants (`200`, `150`) rather than `WINDOW_WIDTH / PIXEL_SCALE` expressions — equal to the old derived values today only by coincidence, which the comment above them says outright so nobody reads a relationship back into two numbers that no longer have one. *Verify, done exactly as written:* doubled both constants to `400`/`300`, rebuilt, launched the exe and painted a streak of Sand across the window with synthetic mouse input. **The whole world squashed to fit, as predicted** — each cell rendered at roughly half its normal size (2 screen px instead of 4, since `SDL_RenderCopy`'s null/null rects always stretch the entire grid-sized texture across the entire window, whatever the grid's size), which is the visual signature of more cells having been packed into the same window rather than the constants merely being renamed. Reverted to `200`/`150` immediately after confirming it, since this step ships the decoupling, not the doubled size. The src rect that turns "the whole world, squashed" into "a real view of part of a world" is F3.3; the mismatch between `PIXEL_SCALE`-based mouse math and the world's actual on-screen scale is left exactly as broken as the roadmap said it would be — that is `Camera`'s job, next.
- [x] **F3.2 — One `Camera`, and only it knows `PIXEL_SCALE`.** New `src/game/camera.h` — `world_to_screen`, `screen_to_world`, and `cell_size()` (for a one-cell rect's width/height, where `world_to_screen(1)` reads oddly). `PIXEL_SCALE` is now `Camera::SCALE`, a private-to-the-file constant, and every site in `main.cpp` that used to touch it directly goes through a `Camera camera;` instance instead: the mouse-to-grid conversion (screen-to-world, feeding both the brush and the dig aim), the player body rect, and the aim-marker rect (both world-to-screen). *Verify, done exactly as written:* `grep PIXEL_SCALE src/` now matches only `camera.h` itself — the check the step names. Clean build, no new warnings, all five suites still pass at 130 checks unchanged (`main.cpp` isn't under test, so this is a pure refactor `ctest` can't see either way). Confirmed live: launched the exe, right-click-dragged a streak of Sand that landed exactly under the mouse path, held `D` to walk the player and watched its rect track the same screen position the old raw multiply would have produced, and left-clicked to dig into the settled pile with the orange aim marker correctly clamped to `DigTool::RANGE` — nothing about brush, movement, or dig aim shifted, which is the expected result of a coordinate refactor that changed no values, only where they live.
- [x] **F3.3 — Upload only what is visible.** New `VIEWPORT_WIDTH`/`VIEWPORT_HEIGHT` (`WINDOW_WIDTH`/`HEIGHT` divided by `Camera::SCALE`) size the streaming texture instead of `GRID_WIDTH`/`GRID_HEIGHT`, and the per-frame upload is now one `SDL_UpdateTexture` call against a rect clamped to `min(viewport, grid)` — the source pitch stays the grid's real row width even though the rect is narrower, so SDL reads the right columns straight out of the full pixel buffer with no intermediate copy needed. The visible rect's origin is pinned at `(0, 0)`; this step is only the upload half, not the panning half — camera following stays F3.4's job, so today's default 200x150 grid still renders as the whole world, unchanged, because `GRID_WIDTH`/`HEIGHT` still equal `VIEWPORT_WIDTH`/`HEIGHT` by the same coincidence F3.1 named.
    - *Verify.* Clean build, no new warnings, all five suites still pass at 130 checks unchanged (`main.cpp` isn't under test). **`grid_bench` genuinely cannot see this, exactly as this step's own text warned** — it links no SDL and never calls `SDL_UpdateTexture` — so the number came from a temporary probe timing only the `SDL_UpdateTexture` call itself via `SDL_GetPerformanceCounter`, removed again after measurement rather than kept as permanent instrumentation. Title-bar fps was not usable as the signal: the renderer is created with `SDL_RENDERER_PRESENTVSYNC`, so frame rate stayed flat regardless of upload cost. Bracketed at a 2x world (400x300): the old whole-grid upload averaged ~42-52 us, the new viewport-clamped upload ~18-19 us; at a 4x world (800x600) the new path stayed at ~17-19 us, unchanged. That is the actual claim this step makes — not "faster today," since today's grid equals the viewport and there is nothing to clamp, but "no longer scales with world size, by construction." Not recorded in `PERFORMANCE.md`'s table, per this step's own instruction not to mix an in-game reading into the bracketed bench numbers.
    - **Confirmed live**, through the new viewport-sized texture: right-click-dragged a streak of Sand that landed exactly under the mouse path and settled normally, then left-clicked to dig into it with the orange aim marker correctly aligned — both are what would break first if the rect or source-pitch math were wrong. Player movement was not re-exercised in this pass: synthetic keyboard input (`keybd_event`) did not reach the window in this environment, confirmed independently since the material-switch keys didn't register either — a test-harness limitation, not a gap in this step, since the diff touches no input or player-rendering code. The player's rect was visible and correctly positioned in every screenshot regardless.
- [x] **F3.4 — Follow the player, clamped at the world edges.** `Camera` gains `follow(center_x, center_y, viewport_w, viewport_h, world_w, world_h)`, which centers the viewport on the player and clamps to `[0, world - viewport]` per axis — a world no bigger than the viewport on an axis (every world in the project today) clamps to 0 on that axis, i.e. no scrolling, which is why the default 200x150 grid still looks exactly like F3.3 left it. Because the viewport's offset can now differ per axis, `world_to_screen`/`screen_to_world` split into `_x`/`_y` variants, and a new `scale_length()` covers the two call sites that were converting a *size* (`Player::WIDTH`/`HEIGHT`), not a position — those must never have the offset subtracted, only F3.2's original scale, or the player's rect would shrink and grow as the camera panned instead of just moving. `main.cpp` calls `camera.follow(...)` once per rendered frame using the player's current center, at the same point mouse and keyboard are already sampled once per frame, and the F3.3 texture upload now reads from `camera.view_x()/view_y()` instead of a hardcoded `(0, 0)`. Feel work (deadzone, smoothing, look-ahead) stayed out of scope, exactly as written.
    - *Verify.* Clean build, no new warnings, all five suites still pass at 130 checks unchanged (`main.cpp` and `camera.h`'s new methods aren't under test). Confirmed live at the default 200x150 grid: right-click-painted Sand and left-click-dug it, both landing exactly where aimed — proof the offset-aware `_x`/`_y` split didn't change anything when the offset is always 0, which is the case every existing scenario actually exercises.
    - **Confirmed live at a 2x world (400x150)**, the only way to actually see this step do something: a temporary build force-set `input.right = true` every frame, since synthetic keyboard input (`keybd_event` and, after investigating further, `SendInput` with a correctly-sized `INPUT` struct) does not reach this window in this environment, confirmed independently on a completely unrelated key (`Escape`, which should quit and didn't) — an environment limitation, not specific to movement keys, so an auto-walk build was the only way to move the player at all for this pass. Two screenshots, both reverted afterward along with the grid size and forced input: near spawn, the player sits centered in the window (screen x ~400, matching `world_to_screen_x` of a viewport-centered player); after several seconds of continuous rightward movement, the player is instead pinned near the *window's* right edge rather than staying centered — the visual signature of the camera having hit `max_view = world_w - viewport_w` and stopped scrolling while the player kept walking. That is what "clamped at the world edges" means and it is what happened.
- [x] **F3.5 — Answer off-screen simulation once, in writing.** Everything simulates, always, regardless of the camera. The full grid steps every fixed tick no matter what the viewport currently shows; the viewport is a *render*-only concept, introduced in F3.3/F3.4 to bound upload and draw cost, and it stays that way. The moment it also started deciding whether a chunk gets to step, it would quietly become a second scoping mechanism competing with chunking — unnecessary, since chunking already makes an idle region cost nothing on its own, on-screen or off, and actively wrong for the region that *is* active: an avalanche the player triggers and walks away from, or Fire spreading through a structure off-screen, would freeze the moment it left the viewport and resume the moment the camera swung back. That is not a performance optimization, it is a correctness bug wearing one. **The sharper argument is determinism, not performance.** F1.7 holds `Grid` to being a pure function of its seed. If simulation ever depended on camera position, two players on the same seed and the same input log could diverge in world state purely because they were looking at different things when it mattered — exactly the failure mode F1 exists to rule out, reimported through a feature that was only ever supposed to touch rendering. Chunking is what makes "always simulate everything" affordable enough to answer this way: a sleeping chunk already costs nothing, so there is no performance case left to make for culling by visibility, only a correctness case against it.
    - **This also answers the open half of Presentation & Tooling's Resolution options item without re-deciding it there.** World size and window/resolution size are already independent (F3.1); this step adds that they stay independent in *both* directions — a bigger window reveals more of a world that was always fully simulated, it never changes how much of that world runs. Resolution options is free to grow `VIEWPORT_WIDTH`/`HEIGHT` (and, with it, the render-only cost F3.3 bounded) without touching `GRID_WIDTH`/`HEIGHT` or `grid_bench`'s numbers at all.
    - *Verify.* No code in this step, on purpose — the check is that the answer is written down before anything that needs it (the resolution item, a future level-size decision) could otherwise re-derive it, inconsistently, on its own.

### F4 — A way to get a level into the grid
*Was a clause inside a Presentation & Tooling item, which made it unreachable. Promoted, not expanded.*

**This one was a circular dependency in this document, not a missing idea.** Quantum Worlds is the first Medium Term item and cannot exist without some way to populate a grid — nor can Objective + Extraction, which needs somewhere to put an objective. The loader that does it was written down only inside **First authored scene + art pipeline**, in Presentation & Tooling, a section whose own header says nothing in it starts while Medium Term is open. So the slice's first item needed a thing that was forbidden to build until the slice was finished. The note itself already spotted this and says so at the bottom of [notes/art_pipeline.txt](notes/art_pipeline.txt); the roadmap did not.

**Scope is phases 0-2 of that note and nothing else** — about 160 lines. Phases 3-6 are the parts that make an authored scene *cohere* (backdrop, palette retune, player sprite), and they are presentation work that stays in Presentation & Tooling. The pixel art **editor** stays deferred and is not touched here; an external editor plus this loader is the whole authoring story until art volume is measurably the bottleneck.

It is deliberately agnostic about where levels come from. The same loader serves a hand-painted scene and a generated one, which is why it belongs here rather than inside either feature.

- [x] **F4.1 — `Grid::paint(x, y, type, colour)`.** Placement with an explicit colour instead of a jittered one, which is what lets authored art keep its own pixels. `set_element` and `paint` now share one private `place()` that does the actual write — `mark_dirty` and the structure-removal support check live in exactly one place, so the two paths cannot drift apart the way a copy-pasted second implementation invited. *Verify, done exactly as written:* `test_grid.cpp` paints a cell and checks the colour comes back unjittered. **The wake check was rewritten rather than left as the `active_chunk_count() > 0` proxy it shipped with** — that proxy passes for any write at all and says nothing about *this* write's colour path, so it could not actually have caught a missing `mark_dirty`. The real check paints an unsupported Sand cell (a powder, so its fall is driven by the ordinary per-step movement rule rather than the structural support queue — painting Wood instead would have asserted something false, since a freshly placed structural cell is deliberately left standing so a scene can paint a platform without it collapsing on load) and steps the world: it lands at the bottom of the grid, still carrying the colour it was painted with.
- [x] **F4.2 — Scene format and a headless loader in `src/scene/`.** Two same-size buffers, a material map and an albedo, in `src/scene/scene.h`/`scene.cpp` — SDL-free. `load_scene(grid, scene, offset_x, offset_y)` skips cells whose material is `Empty` rather than painting them: `Empty` has no legend colour, so its albedo pixel is an accident of whatever the art file holds there, not an authored choice, and painting it anyway would mean stamping a scene onto a grid that already had something in it silently blanked the untouched-looking background back out. *Verify, done exactly as written:* `test_scene.cpp` builds a 2x2 buffer pair in memory, loads it at an offset, and asserts the grid matches cell for cell — no file I/O. A second case added for the `Empty`-skip rule: paints a cell, loads a 1x1 `Empty` scene on top of it, and checks nothing changed.
- [x] **F4.3 — BMP decode in `main.cpp`, loaded at startup.** `load_scene_from_bmp` reads two BMPs via `SDL_LoadBMP`, converts both to `ARGB8888`, and maps each material pixel to an `ElementType` by exact RGB match against `MATERIALS`. **Shipped broken and is the one step this pass had to actually fix, not just verify.** Three separate problems, each alone enough to sink it: the pixel loop indexed converted surfaces as `y * width + x`, ignoring `SDL_Surface::pitch` — silently wrong the moment a converted surface pads a row, which `ARGB8888` at these widths happens not to; the scene was authored at 640x400 while `GRID_WIDTH`/`GRID_HEIGHT` were still 200x150, so every one of its cells landed outside the grid and `paint`'s bounds check dropped the entire scene without a warning; and the BMPs were loaded by a path relative to the working directory with no asset-copy step, so a normal launch from `build/Debug/` couldn't find them at all. Fixed all three: the pixel loop now walks each surface's own `pitch`; `GRID_WIDTH`/`GRID_HEIGHT` are now `640`/`400` to match the authored resolution (deliberately bigger than the 200x150 viewport now, which is what exercises the panning half of `Camera::follow` — F3.4 — rather than just the decoupling F3.1-F3.3 landed); and a `POST_BUILD` custom command in `CMakeLists.txt` copies `assets/` next to the built executable. *Verify:* launched `SlopPhysics.exe`, screenshotted it a few seconds in, and confirmed the authored terrain rendered — fence post, the bridge over its pit, a jump ledge, and the water channel all visible, with the player having fallen from spawn onto the bridge beam and stopped there, which only happens if the painted cells are real collidable terrain and not an empty grid.
- [x] **F4.4 — Make the first scene a test fixture wearing art.** `generate_test_scene.py`, rewritten — the version this replaced was four flat rectangles that exercised nothing named below, at a resolution that didn't even match the grid. The new layout, at the authored 640x400: an uneven Sand slope built from randomized 1-3 cell treads (blocky stairs, not a smooth ramp — a smooth ramp doesn't ask anything different of the player's step-up than flat ground) for powder step-up; three standalone Wood fence posts standing on the floor for dig-the-base collapse; a real pit (floor genuinely absent, not just visually implied) spanned by two grounded pillars and a beam, so digging one pillar exercises the support flood fill carrying the beam and the far corner down together rather than shedding it — the L-piece case; a walled water channel with an open top and a diving ledge above it, for density and falling through a liquid; a row of Wood sleepers beside the channel, close enough that breaching the wall with the dig tool lets the spilled Water reach them, for the ignite-with-Fire-then-douse-to-Steam case; and four jump ledges at mixed heights for arc tuning. *Verify:* regenerated `assets/test_material.bmp`/`test_albedo.bmp`, decoded the material BMP and confirmed every legend colour matches `MATERIALS` exactly (a near-match loads silently as `Empty` and would defeat the whole scene the same way the size mismatch in F4.3 did) and that the pit has no floor under it, and confirmed the player's spawn point stays clear of everything placed.

*Techniques this project deliberately does not need (entity/component system, threading, networking, a scripting layer, a bespoke asset editor, shrinking cell size) are recorded in [ENGINEERING_NOTES.md](ENGINEERING_NOTES.md) so they aren't mistaken for pillars when the itch to add one arrives.*

## 🟤 Engine & Visual Depth (before Medium Term)

*Nine items across three tracks: three that deepen the simulation, four that make the presentation cohere, two that pay for the first three. **Nine is a ceiling, not a starting point** — the same budget language Foundations carries, and for the same reason.*

**This section exists because the product changed shape, not because the plan lost its nerve.** `VISION.md` used to say to treat engine work as a means to the slice and stop extending it the moment the slice did not require more. That sentence is gone, and its replacement says what is actually true now: **the engine and its visual design are the selling point.** A pixel-physics game competing for attention against everything already on Steam is bought on how its world behaves and how it looks doing it, and neither of those is a coat of paint applied after the loop works. Scope discipline is not weakened by this and is not meant to be — it still applies in full, and it is still pointed exactly where `notes/reality_check.txt` aimed it: the Ideal Systems wish list in `VISION.md`. Factories and a simulated stock market are still arithmetic this project cannot afford. Depth in the pillar is not the same kind of spending, and conflating the two is what would actually cost the project its identity.

**Every item in the E track is traceable to something observed in play, not to an idea someone had.** That constraint is the whole reason this section is allowed to exist, and an item that cannot name its observation does not belong here:

- **Water never levels.** `Grid::can_displace` refuses every upward move unless the mover is lighter than its target, and `Empty` has density 0 — so a liquid can *never* rise, under any circumstances. F4's water channel cannot equalize across a U-bend, and water reads as a powder that happens to spread rather than as a fluid. → **E1**
- **Fire is random, not hot.** Ignition is a per-cell dice roll out of the `REACTIONS` table. Nothing in the world has a temperature, nothing warms, nothing cools, and so a fire spreads by luck rather than by heat — which is why it never looks like it is *burning through* anything. → **E2**
- **Collapses look stiff.** `drop_component` translates an unsupported piece straight down with its shape perfectly intact. Masonry descends like an elevator instead of coming apart. → **E3**

*Order is **E → V → P**, and both arrows are load-bearing. **E before V** because E2 adds columns to `MATERIALS` and changes what Fire and Water mean; V2 retunes that same table's colours, and tuning a palette for materials whose behaviour is about to change means tuning it twice. **P after both** because P1 is a memory-layout change and the layout cannot be settled before the field set is — optimising the cell array and then adding a temperature byte to it is the same mistake in the other direction. The cost of that ordering is real and is named here rather than discovered later: E2 adds per-cell per-step work to a loop already at 71% of frame in its worst case, so **each E item carries its own bracketed measurement** per [PERFORMANCE.md](PERFORMANCE.md), and if any one of them alone breaks the budget, P1 gets pulled forward ahead of the rest.*

### E — Simulation depth

- [x] **E1 — Liquids find their level.** A blocked liquid cell with `Empty` directly above it — a surface cell — searches its own connected body for another surface at least 2 rows lower, and moves there. Bounded at 64 cells of search (`MAX_PRESSURE_CELLS`) and gated at 2 rows of difference (`MIN_PRESSURE_HEAD`), both in `grid.h` with the reasoning next to them. A U-tube converges to dead level in under 50 steps and then sleeps.
    - **The direction of the move is the decision, and the obvious direction is the one that does not work.** The first implementation was the one the item's own wording implies: let the short arm *rise*. It does equalize — for two cells, and then it stops. Rising is a swap, so it leaves a bubble of `Empty` inside the body, and the transfer is not finished until the ordinary fall and spread rules have walked that bubble back down the arm, along the join and up the far side. That takes twenty-odd steps, and while the bubble is in the join it **cuts the body in two**, so the pressure search transiently answers "no head", the cells stop marking themselves dirty, the chunk sleeps, and the U-tube parks itself two cells out of level with nothing awake to notice. Every fix for that is some way of keeping an unlevel body awake, which is a standing cost charged to every pool in the world to serve the one that is out of level. Moving the *tall* cell down onto the low surface instead makes each transfer a single atomic swap: no journey to stay awake for, and the wake-up is free and local, because the 3x3 mark on the vacated cell is exactly the cell below it — which is the next surface cell and the next one to move. The visible cost is that a cell can travel further than one cell in a step, bounded by the search, always within one connected body of one liquid; what it looks like is one side dropping while the other rises, which is what a U-tube does.
    - *Verify, six checks (155 total).* The U-tube converges (`left surface row=31 right=31`) and **conserves water** — 25 cells placed, 25 after, which is the check that actually matters, since the obvious way to make water level is to invent some and a rule that equalizes by creating cells passes the level test and fails the engine. A third check that the roadmap did not ask for and should have: the U-tube is **asleep** once level. Level is only half of it — a rule that equalizes and then trades cells back and forth across the join is level on average and costs full price forever, and `MIN_PRESSURE_HEAD` being 2 rather than 1 is the entire reason it does not. Then the negative case, three checks on a settled flat pool: it does not climb, it conserves, and it goes back to sleep — every surface cell of a settled pool asks the pressure question on every step it is awake, and has to keep answering no.
    - **The fixture was wrong twice before the engine was right once, and both times it was the collapse rule behaving exactly as specified.** A U-tube drawn as walls in mid-air is a structure standing on nothing: the first swap of water inside it queues a support check and the whole container sinks through its own contents. Rebuilt on the world's bottom row — and it *still* dropped a cell, because the divider between the two arms sits over the join, so it is a slab hanging in mid-air even when the outer walls are not. It needs a lid to connect it to something grounded. Worth recording because both failures presented as "the water did something strange", and neither was about water.
- [x] **E2 — Heat, the seventh axis.** The one deliberate pillar addition in this document, and it is deliberate: Foundations was explicitly forbidden from adding a seventh axis, that prohibition was correct for F1–F4, and this is the considered decision to spend one — bought by a named observation rather than by an itch. A `uint8_t temperature` on `Element`, thermal columns on `MATERIALS` (ignition point, conductivity), conduction between neighbours, and `REACTIONS` re-expressed as temperature-gated rather than pure chance: Wood ignites above its threshold instead of on a dice roll, Water absorbs heat and boils, Steam condenses as it cools. Three traps, each a known failure mode of something this engine already does:
    - **It should be free in memory, and that has to be confirmed rather than assumed.** `Element` today is `type`(1) + 3 pad + `color`(4) + `updated_tag`(1) + `fall_ticks`(1) + 2 pad = 12 bytes, so offsets 10–11 are padding the struct already carries and a temperature byte lands there at no cost. Confirm it with the `static_assert` in `element.h`, the same way `ENGINEERING_NOTES.md` corrected its own 8-vs-12-byte claim — **by compiling it, not by counting fields**, because that entry was wrong for several revisions precisely from counting fields.
    - **A cell whose temperature is changing must stay awake.** This is the write rule and the wake rule wearing new clothes: heat that diffuses into a sleeping chunk and stops is the same bug as material frozen in mid-air, and the same bug as the boxed-in Fire cell that froze forever before its self-wake fix. Expect to need the equivalent of that self-mark for any cell not yet at equilibrium.
    - **Integer arithmetic only.** Floating-point diffusion would put cross-platform nondeterminism back into `Grid`, which is the exact property F1 spent seven steps establishing and F1.7 wrote down as an invariant.
    - *Built as:* a `uint8_t temperature` on `Element` defaulting to `AMBIENT_TEMPERATURE` (20, on a scale read as degrees Celsius so the constants mean something to a person); three thermal columns on `MATERIALS` — `conductivity`, `spawn_temperature`, `heat_source`; and `Grid::step_thermal`, one pass per awake cell riding the existing sweep. `REACTIONS` rows gained a `min_temp`/`max_temp` window, and Wood and Oil moved from `{catalyst: Fire, chance: 12%/40%}` to `{spontaneous, 100%, above 120/90}`. **The ignition point lives on the reaction rather than on the material**, which is a departure from this item's own wording: a threshold is a property of a transformation, not of a substance, and Water needed exactly the same field to boil. All three traps above were real and all three were paid.
    - **The memory claim held, and it was checked the way this item demanded.** `sizeof(Element)` is still 12 — `static_assert` in `element.h`, plus a runtime check in `grid_test` so that an assertion which quietly stops being tight is visible. Counting fields would have given the same answer here; that is not the point, since counting fields is exactly what made `ENGINEERING_NOTES.md` wrong for several revisions.
    - **Four neighbours was the wrong number, and the burning-beam test is the only reason that is known.** Conduction was written orthogonal-only, borrowing E1's argument that a diagonal step lets two bodies touching at a corner exchange through a seam with no area. That argument is about moving *matter* and does not transfer. An ignited Wood cell becomes Fire, Fire is a gas, so it rises out of the beam on the next step — and the flame that should light the next cell along is then sitting diagonally above it and nowhere else. **The fire front stalled after exactly one cell**, and no conductivity would have fixed it, because heat cannot cross a gap the rule says does not exist. Eight neighbours also puts heat on the same neighbourhood as `has_neighbor` and `mark_dirty`, so contact means one thing throughout the engine. What caught it was the negative half of a test written to check that a beam burns *through* rather than lighting up all at once.
    - **The first measurement said 18% and that number was not accepted.** Bracketed on/off/on in one binary per [PERFORMANCE.md](PERFORMANCE.md), the full pass cost `cascading` +18% and `churning` +32%, putting the stated number to watch at **88% of a 60 Hz frame** against 71% before. This document's own rule says P1 gets pulled forward if one E item breaks the budget. It was not invoked, because the cost was almost entirely waste: `cascading` contains no fire and never gets warm, and it was paying eight neighbour probes per awake cell per step to confirm that nothing had changed. **A cell at exactly ambient now does no thermal work at all**, which is exact rather than approximate — conduction writes both ends by the same amount, so it does not matter which of a pair initiates it, and nothing can be off ambient and asleep. That took the overhead to **+2.2%** on `cascading` and **+2.7%** on `churning`, with `burning` — the scenario that actually uses heat — at +29% of 3.3% of a frame. The lesson is worth more than the win: **the first honest measurement of a feature is a measurement of the first implementation of it, and reaching for the escape hatch in the plan before reading what the number was made of would have spent a whole roadmap item to avoid one comparison.**
    - *Verify, fourteen checks (169 total).* Ignition is no longer statistical, and that is the observation E2 answers rather than a tidier test: the old assertion was thirty independent seeds and a bar at 40%, because a 12%/step roll raced Fire's own 6% burnout and could correctly fail. It is now a threshold — Wood catches at step 11, Oil at step 8, same answer every run, and *the number of steps* is finally a quantity worth asserting on, which is precisely the state between "wood" and "on fire" that was missing. Then: heat conducts out of a flame and **stops** (near 180, far 20 along the same bar); a beam burns away at the lit end while the far end stays Wood at ambient; water boils; steam condenses and conserves matter doing it; the byte is free; and **a burnt-out world cools back to ambient and goes to sleep** — the check the whole axis stands on, since heat that never settles keeps a chunk awake forever and does it silently, a warm world being indistinguishable from a cold one to look at.
    - **Three existing tests had to change, and each change is E2 being right rather than a test being loosened.** "Water extinguishes fire into steam" read Steam at 30 steps before heat existed and Water at 5 steps after — a puff pinned against cold stone dumps its heat into it and condenses — so it now asserts the cell *passed through* Steam, which is what the test was ever about. "Steam rises" came down from 300 steps to 100, since steam does not last 300 any more. And the steam-condensing fixture moved out of a sealed Wall box into open air: `Empty` has conductivity zero, so steam in air cools only by the slow bleed and lives a couple of hundred steps, where steam packed against stone is gone in ten. Both are correct; the one worth asserting on is the one a player sees.
- [x] **E3 — Collapses break instead of dropping rigid.** Answers the observation with **fracture rather than rotation, and that substitution is the decision, not an approximation of one.** A piece whose support is uneven splits along the stress into separate pieces that fall independently, reusing the support flood fill that already exists. True rigid-body rotation on a cell grid means resampling the piece every step it turns, which destroys the exact authored pixels [notes/art_pipeline.txt](notes/art_pipeline.txt) calls the entire visual pillar — the feature would be bought by breaking the reason the engine is interesting. Fracture costs nothing in that currency, and masonry mostly *breaks* rather than tips anyway. Toppling is not dropped: a small piece that fracture has already separated is the cheap candidate for it, and that is the follow-on this item is meant to make affordable. *Verify:* the existing "a piece resting on solid ground never moves" tests still pass, unchanged. `Grid`'s own comment on `MAX_SUPPORT_CELLS` says a missed collapse is invisible while a wrong one turns a level into rubble — fracture is the single change in this document most capable of violating that asymmetry, so it is checked against the negative case first and the positive case second.
    - *Built as:* a `uint8_t piece_tag` on `Element`, and `Grid::fracture_landing`. The support flood fill only crosses between two structural cells whose tags match, so **a crack is stored as a disagreement between two cells** rather than as a line between them — which is what lets it survive the piece moving, since `swap_elements` carries whole Elements. `is_grounded` was widened to match: "more of the same structure is not support" now means the same *piece*, so a fragment resting on the slab it broke off is standing on something.
    - **A piece breaks when it lands, not while it falls, and that timing is the entire safety argument.** Every "nothing must move here" test in the suite is about a piece at rest, and a piece at rest has `fall_ticks` of zero and never reaches the code at all. Fracture cannot *start* a collapse; it can only let one that was already happening finish unevenly. That is the asymmetry above kept by construction rather than by care, and it is why the pre-existing negative tests passed untouched on the first run.
    - **Splitting a falling piece would have been a no-op, which is worth recording because it was the obvious design.** Break a piece in mid-air and both halves are unsupported, so both fall by exactly one cell on exactly the same steps — identical to not having split it — and the next fill re-discovers them as one component because they are still touching. **Fracture without persistent state is impossible on this representation.** The crack has to survive into the next fill or it never had any effect, and that is what the byte buys. It was the last one: `Element` is now 12 bytes with no padding left, so the next field added there is the first that actually costs memory.
    - **Where the crack goes was wrong the first time, and the failure was silent.** The first rule put it near the middle of the piece with a random offset, on the theory that masonry breaks somewhere arbitrary. It does not work: a break only *does* anything if it separates a part that is held up from a part that is not, and anywhere else both fragments rest on the same ground, so nothing moves and the only trace is a tag nobody can see. A 60-wide slab dropped across a 30-wide step broke at column 55 and looked exactly like no fracture at all. Reading the ground instead makes this item's own words literal — the crack is the line between the columns that landed on something and the columns that landed on nothing — and it has a second benefit that was not the reason for it: **a piece landing flat on flat ground does not break at all**, so routine landings stop burning tags out of the 255 there are.
    - **Two more bugs, both of which presented as "fracture is not firing".** The fill that identifies the landed piece cannot follow structure, because the instant a slab touches the floor the two *are* one structural component — it walked out of the slab, across the whole floor, past `MAX_SUPPORT_CELLS`, and gave up. The piece is identified by being **in flight** (`fall_ticks != 0`) instead. And the landing has to be detected at the fill's *seed*, not at the grounded cell it ends on: `is_grounded` refuses to count more of the same structure, so a slab settling on a stone floor is not grounded where it touches, and the fill answers "grounded" a hundred cells below at the bottom of the world, on a cell that has never moved. Three wrong guesses in a row, all failing quietly and identically from the outside, which is the argument for instrumenting rather than reasoning about the fourth.
    - *Verify, ten checks (179 total).* The negative case first, as this item demanded: a large piece at rest is never broken, digging a hole through a grounded slab does not break the rest of it, and a piece that drops a single cell onto the floor lands intact — `FRACTURE_MIN_TICKS` is what separates "it tipped off a ledge" from "it came down". Then the positive case: a slab dropped across a step ends at two heights instead of one rigid level, which **doubles as the proof that a crack persists**, since the overhang can only descend if the *following* step's fill refuses to cross the crack. Plus conservation on every path, and "the wreckage comes to rest" on each, because a heap that keeps re-deriving its own support costs full price forever.
    - **One existing test asserted the opposite of this feature and was re-expressed rather than deleted.** "A round piece is unchanged in shape once it has landed" — a boulder that falls 280 cells and arrives pristine is the elevator problem in its purest form. What still has to hold is that it *broke* rather than *smeared*, so it now checks that nothing was lost and that no fragment is left grinding against another. Its sibling, "unchanged in shape *while falling*", is untouched and is still the check that a piece moving eight cells a step does not shear.
    - **The benchmark could not price this and said so only under questioning** — `collapsing` drains its slabs *before* they land, by design and in its own comment, and fracture fires on landing and nothing else. The first bracketed A/B ran the feature zero times and returned a confidently flat number. A `shattering` scenario was added to `bench_grid.cpp` to close the gap; see [PERFORMANCE.md](PERFORMANCE.md).
    - **What this does not do, named rather than left to be discovered.** A piece breaks at *one* boundary per landing, so a slab coming down across four ledges breaks in two, not five — the fragments are asked again only if they fall again, which for a shallow ledge they do not. Toppling is still not here, and is still the follow-on this was meant to make affordable.

### V — Visual foundation

*Phases 3–6 of [notes/art_pipeline.txt](notes/art_pipeline.txt), promoted out of Presentation & Tooling. **The mechanism is F4's, not an exception:** the items leave that section rather than that section's "nothing starts here yet" rule bending for them. The rule is intact and still governs everything left behind it.*

- [ ] **V1 — Transparent `Empty` and a backdrop layer.** `Empty` becomes `0x00000000` and the streaming texture gets `SDL_SetTextureBlendMode(SDL_BLENDMODE_BLEND)` — confirmed absent from `main.cpp` today, so the cell texture currently paints opaque black over anything drawn behind it. Two lines that affect no physics, and **nothing else in this track composites until they land**, which is why it is first.
- [ ] **V2 — Palette and jitter pass on `MATERIALS`.** Table only, no engine change. Retune the base colours out of saturated debug primaries — Water `#4444FF` and Sand `#EEDD82` will read as a debug view against authored art no matter what else is done — and cut `color_jitter` hard, because random per-cell noise fights hand-placed ordered dithering rather than adding to it. Note what this does *not* cover: reaction products still take their colour from the table, so authored wood burns into generic fire, and that seam is the reason the palette has to be chosen as a set rather than per row.
- [ ] **V3 — Player sprite decoupled from its hitbox.** Collision stays 4x8 — it is tuned to the physics and a suite of tests stands on it — and a larger sprite is drawn anchored to the box's bottom-centre with an offset. Touches no physics and breaks no tests, and it is what puts the player at a believable proportion of screen height instead of reading as a 4x8 white rectangle.
- [ ] **V4 — Props overlay and the `Snow` material.** The non-simulated props layer (signage, fencing, anything that exercises no system and therefore belongs on top rather than in the grid), plus `Snow` as one `MATERIALS` row: a powder lighter than Sand, and the single most on-theme material for the reference art. **Sequenced after E2 on purpose** — with heat in the engine, snow melting into water is a thermal row rather than a special case someone has to hand-write, which is the whole argument for E2 arriving first.

*Per-cell emissive lighting — fire and hot material actually casting light, terrain shadowing itself — is the part of this that would genuinely read as cutting-edge, and it is **deliberately not in this section.** It is the next visual item after this tier, gated on there being authored art worth lighting: lighting an unlit debug palette flatters nothing, and E2 is what makes "which cells are emitting" a question the engine can already answer.*

### P — Performance

- [ ] **P1 — Split the cell array hot from cold.** [PERFORMANCE.md](PERFORMANCE.md) already establishes that `cascading` — 71% of a 60 Hz frame, and the stated number to watch — is **bandwidth-bound**, and `ENGINEERING_NOTES.md` prices a 12→2 byte cell at roughly 6x the hot loop's memory traffic while rejecting it outright as mutually exclusive with authored per-cell colour. **Splitting the array captures most of that win without giving up authored colour, which is the reason to reach for this one first:** `type` / `updated_tag` / `temperature` are read for nearly every awake cell every step, `color` is touched only when a cell is written and when the frame is uploaded. Two documents currently call that trade impossible; this is the thing that makes it not. Measured bracketed (on, off, on) with `churning` as the control, per the rules in that file — and **recorded honestly if it does not pay**, because both of the previous entries filed as cheap wins failed and were left on the record rather than deleted.
- [ ] **P2 — Re-baseline the benchmark at the size the game actually runs.** `grid_bench` measures 960x540, the resolution the project is aiming at. F4.3 moved `GRID_WIDTH`/`GRID_HEIGHT` to 640x400, and nothing measures that. Add it alongside rather than instead — the target number is still the one that matters for shipping, but a budget nobody checks against the size actually being played is a budget in name only.

## 🟡 Medium Term (Core Gameplay Loop)
*Tying the physics engine into the lore. This section is the v0.1 slice.*

*This list is longer than it used to be, and every addition came from an audit rather than from an idea. Three items were previously absent even though the Definition of Done depends on all three — a camera, a save file, and a way to die. A fourth, the UI decision, was sitting in `ENGINEERING_NOTES.md` as a warning with nothing scheduled to act on it. None of those were deferred decisions; they were gaps, and a plan that hides its prerequisites is not a plan. Assume the slice is at least twice the work the old five-line version implied. Two of the additions have since moved out again, upward rather than away: the camera is F3 and the level loader is F4, because neither was ever slice content — they are what slice content stands on.*

***Engine & Visual Depth now sits between Foundations and this section, and that is a deliberate delay to the slice rather than an accident of ordering.** The short version: the engine and how it looks are the selling point, so they are worth depth before the loop is built on top of them — the argument in full is in that section's preamble and in `VISION.md`'s Scope Discipline. Two consequences for the items below. **Quantum Worlds and Objective + Extraction get a better engine to be built against** — heat, levelling liquids and fracturing structures are all things a level would otherwise have to be designed around the absence of. And **player health has one of its two damage sources waiting for it**: "what damages the player" is answered for free by E2, since fire that has a temperature is fire the body can be asked to read, exactly the way collision already asks what it is standing in.*

- [x] **Pick the UI layer, in writing, before anything needs one.** Decided and recorded in `ENGINEERING_NOTES.md`: immediate-mode, drawn directly against `SDL_Renderer`, no new dependency — a library (Dear ImGui, Nuklear) loses for the same reason PNG lost to BMP in F4, and hand-rolled retained-mode loses for the same reason an ECS is off the table, both recorded in full there rather than repeated here. The one gap immediate-mode against `SDL_Renderer` opens on its own is text, since the renderer draws no glyphs — closed with a small hand-authored 3x5 bitmap font (`src/ui/text.h`/`text.cpp`, SDL-side and outside `ENGINE_SOURCES` like `main.cpp`), covering A-Z, 0-9 and a handful of punctuation rather than the whole ASCII table. *Verify, done exactly as written:* the frame-rate/brush/awake-chunk readout that the window title bar carried alone now draws inside the game window itself with the new font, over a translucent backing rect for legibility against whatever the simulation is doing underneath — the window title returns to being a plain, static label. This is the drawing primitive the health readout, pet agent panel and resolution options will each build their own screen on; none of those three screens exist yet.
- [ ] **Quantum Worlds:** A portal/level generation system where the player enters a single "trial". One world, one generator — variety comes later. Needs F1 for a seed, F2 for something to swap, and **F4 for any way at all to get a level into the grid**.
- [ ] **Player health and death.** There is no damage model at all today. Fire is fully simulated and cannot hurt the player, because the grid cannot see the body. "Dies losing the run" is half the Definition of Done and it currently has no implementation behind it. Needs: what damages the player (fire and fall speed are the two the engine can already supply for free), how it is shown, and what death does to the run — which is `Run::reset(seed)` from F2 and should not be a second code path. **Keep the direction of the dependency.** This is the first gameplay system that spans both sides of the rule `tool.cpp` established: the grid does not know about bodies, bodies read the grid. Damage is the player *asking* what it is standing in, the same way collision does. It is not a health field on `Grid` and not a damage column on `Element`, and it will be tempting to make it one.
- [ ] **Objective + Extraction:** One objective type, and a run that ends in either successful extraction or death with loss. Needs F4 too — an objective has to be *somewhere*, and placing it means the level format has a slot for it.
- [ ] **Save and persistence.** The Definition of Done requires the agent to earn coins *idly*, which means progress survives quitting. Nothing in the project writes a file today. Needs a format decision, a save location, and an explicit answer to what happens when a save from an older build is loaded — the cheap answer, refusing to load mismatched versions, is fine and should be chosen deliberately rather than by accident. F1 decides how much of this there is: with a stateless hash the whole of the simulation's randomness is a seed and a step count, so there is no generator state to serialise and no way for a loaded world to quietly diverge from the one that was saved.
- [ ] **The Pet ML Agent:** The UI/system for the pet agent that "observes" the player and gains from completed runs. Deterministic progression, not real ML — see `VISION.md`.
- [ ] **Proof-of-Work Economy:** The idle earning loop — agent performs tasks between runs and earns coins.
- [ ] **Playtest gate:** Put the slice in front of people who did not build it. Do not proceed past this line on the strength of your own opinion. Two things make this cheaper than it sounds and both are earned above: F1 plus F2.3 mean a bug report is a seed and an input log rather than a description, and the thin-hook question raised in `VISION.md` should have been asked *before* getting here rather than answered *by* getting here.

## 🔵 Presentation & Tooling (after the slice, before polish)
*Not slice-blocking — none of this makes the loop fun, and none of it should be started while Short Term or Medium Term is open. Recorded here rather than in the Long Term wish list (`VISION.md`) because these are concrete, bounded, and genuinely expected to get built, unlike that list.*

### Sandbox / debug tooling
*Dev-facing, not player-facing — these speed up iterating on the physics engine itself rather than polishing what ships. Each is small and self-contained in `main.cpp`/`grid.h`, so the risk of scope creep is low, but they still wait behind Short/Medium Term like everything else in this section.*

- [ ] **World reset hotkey (`R`).** Wipes the grid back to Empty and respawns the player, so a messy test scene can be cleared without relaunching the exe. `Grid::reset(seed)` and `Run::reset(seed)` are built and tested in **F2.2**, so by the time this item comes up it is the key binding and a seed to pass — the current world's own seed keeps a debugging session reproducible; a freshly drawn one is the other reasonable choice, and this is where that gets decided.
- [ ] **Pause and single-step (`P` to toggle, `.` to step once while paused).** Freezes the fixed-step loop so a collapse or a reaction can be inspected one frame at a time instead of flying past. The one thing to get right: `accumulator` must not keep growing while paused, or unpausing dumps a burst of queued steps at once.
- [ ] **Continuous brush strokes.** The brush is currently stamped once per rendered frame at the raw mouse position, so a fast drag leaves gaps in the line instead of a solid stroke. Fix is to track the last painted grid position and stamp the brush along the segment to the current one each frame (Bresenham), not just at the endpoint — which also gives straight-line drawing for free on a deliberate fast drag.
- [ ] **Brush outline preview.** Draw a hollow ring at `brush_size` under the cursor at all times, not just while right-click is held, so the brush footprint is visible before paint is committed. Reuses the existing circle math from the paint loop, just as an outline test instead of a fill.
- [ ] **Cell inspector in the title bar.** Extend the existing once-a-second title string with the material name under the cursor via `grid.get_element(gridX, gridY)` — cheap, since that title update already happens on a timer.

### Window and display

- [ ] **Display modes: fullscreen, borderless, windowed.** Industry-standard behaviour, which is more than three `SDL_SetWindowFullscreen` calls: exclusive fullscreen vs. borderless-windowed as separate options, alt-tab that doesn't corrupt the window state, the choice persisted between launches, and correct behaviour when the mode is changed mid-run rather than only at startup.
- [ ] **Resolution options, including 1920x1080, 2560x1440 and 3440x1440.** **The hard part of this item is already answered and should not be re-litigated here.** It used to read as an open modelling question, because the simulation grid was derived from the window size (`GRID_WIDTH = WINDOW_WIDTH / PIXEL_SCALE`) and a bigger window therefore meant a bigger, more expensive world rather than a bigger view of the same one. F3.1 separated the two constants, F3.3 sized the upload to the viewport rather than the world, and **F3.5 settled the model in writing**: world size and window size are independent in both directions, so growing the viewport reveals more of a world that was always fully simulated and never changes how much of it runs. F4.3 then made that concrete — the world is 640x400 against a 200x150 viewport, so the camera genuinely pans today. What is left here is the window plumbing itself: the mode list, persisting the choice, and applying it mid-run rather than only at startup. Ultrawide keeps one open question that is *not* rendering — a wider viewport shows more of the world, which is a gameplay-fairness call rather than a display setting, and it is the one thing in this item still worth deciding rather than implementing.
### Art

- [ ] **Custom pixel art and animation generator for game assets.** A tool for authoring sprites and animations that fit the engine's palette and cell scale, so art is produced in-project instead of hand-drawn in an external editor and re-exported. Worth being blunt about the risk: this is a second application, with its own UI, file format and edit loop, and it is exactly the kind of seductive side-build `VISION.md`'s Scope Discipline section warns about. It only earns its keep once there is enough art volume that authoring by hand is measurably the bottleneck. Until then, an external editor plus a small import step is the cheaper answer.
- **Making an authored scene cohere — moved out, nothing left here.** Both halves of [notes/art_pipeline.txt](notes/art_pipeline.txt) have now left this section. Phases 0-2 became **F4** in Foundations, because the loader was a hard prerequisite for Quantum Worlds sitting behind this section's "do not start yet" rule — which made the slice's first item depend on something forbidden to build until the slice was done. Phases 3-6 became **V1–V4** in Engine & Visual Depth, for a different reason and by the same mechanism: they stopped being "presentation, after the loop" the moment the visual design became a stated selling point rather than a finish applied at the end. **The rule this section opens with did not bend in either case** — both times the items left, which is the only move that does not cost the rule its meaning. What remains genuinely deferred from that note is per-cell emissive lighting, and it is named at the end of the V track rather than here, since it follows V1–V4 directly.

### Shipping
*`VISION.md`'s first Project Goal says "production-level" and none of this existed anywhere in the document. Not slice-blocking — but "production-grade" is a claim these five items are the evidence for, and a plan that omits them is quietly redefining the goal.*

- [ ] **Packaging and a release build.** There is no answer today to "how does someone who is not you run this". SDL is already statically linked, which removes the DLL problem, so this is mostly a named artifact, a version string, and a decision about what a release actually consists of.
- [ ] **Build on macOS and Linux at least once.** `ENGINEERING_NOTES.md` has claimed cross-platform support for several revisions on the strength of using no platform-specific code. That is a reasonable expectation, not a verified fact, and the gap between the two is exactly the kind of thing this document is otherwise good about naming. CI would settle it permanently and is worth considering at the same time, since the whole build is CMake and FetchContent.
- [ ] **Crash diagnosis: assertions and a log.** Nothing in the project logs anything and nothing owns "what do we do with a crash we cannot reproduce" — there is already one unexplained `0xC0000409` (stack buffer overrun) from a prior session, seen twice under heavy load and never reproduced. **The payoff here is bought by F1 and F2.3 and should be spent on purpose:** once a run is a seed plus an input log, a crash report is a file that reproduces the crash rather than a description of it. That is also precisely what the playtest gate needs.
- [ ] **Audio.** No sound of any kind, mentioned in this document only as a clause inside the UI note. Correctly deferred — it makes nothing about the loop more or less fun to *test* — but a pixel physics game with no audio is not a production-grade game, and it should be a line item rather than a parenthetical.
- [ ] **Settle on one project name.** The README says "Toop / Xoco (working title)", the window title says "SLOP Pixel Physics", and the repo is `xoco-game`. Three names for one thing. Trivial to fix, and it stops being trivial the moment anything is published under one of them.

The Long Term "Ideal Systems" wish list has moved to **[VISION.md](VISION.md)** — add to it freely, but nothing in it starts before the v0.1 slice is done and playtested.

Deferred technical decisions and their reasoning (cell size, RNG cost, threading, cross-platform status, frame tag wraparound, player/grid interaction, player feel, the UI layer, material hardness, and the write-path invariant) have moved to **[ENGINEERING_NOTES.md](ENGINEERING_NOTES.md)**.
